RewriteEngine On

# Serve existing files/directories normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send React Router routes to the SPA entry point.
RewriteRule ^ index.html [L]
